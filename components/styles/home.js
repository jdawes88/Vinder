import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#7ea',
    alignItems: 'center',
    minWidth: '100%',
    position: 'absolute',
    bottom: -34,
    top: -44,
  },
  text: {
    position: 'absolute',
    top: 100,
    color: 'black'
  }
})